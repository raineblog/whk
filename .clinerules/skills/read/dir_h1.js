// 没有传入参数，执行后，遍历 docs 文件夹下的所有 .md 文件，把他们的第一个 H1 输出到终端。

// 输出格式是比如：

// ```
// ……
// docs\science\probability\2.md 统计学概述
// ……
// ```

// 以此类推，注意第一个 H1 不一定在第一行，但是一定是第一个 `# ` 开头的行。如果文件里面有多个 H1 那么输出第一个即可，后面的不用管。
