# 20251210

1. 已知等差数列 $\{a_n\}$ 的前 $n$ 项和为 $S_n$，$a_6<0$，$a_4+a_9>0$，则使得不等式 $S_n<0$ 成立的最大的 $n$ 的值为？

2. 已知等差数列 $\{a_n\}$ 的前 $n$ 项和为 $S_n$，且 $S_8<S_{10}<S_9$，则满足 $S_n>0$ 的正整数 $n$ 的最大值为？

3. 若 $\{a_n\}$ 是等差数列，首项 $a_1<0$，$a_{203}+a_{204}>0$，$a_{203}\cdot a_{204}<0$，则

    <div class="grid" markdown>

    A. $a_{204}>0$

    B. $d<0$

    C. $S_{405}<0$

    D. $S_{406}>0$

    </div>

4. 已知正项等差数列 $\{a_n\}$，$a_1=2$，$a_2a_3=24$，求 $\{a_n\}$ 的通项公式；设

    $$
    b_n=\begin{cases}2^{\frac{a_n}{2}}&n\space\text{为奇数}\\ a_n&n\space\text{为偶数}\end{cases}
    $$

    求数列 $\{b_n\}$ 的前 $2n$ 项和。
